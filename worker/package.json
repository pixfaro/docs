{
  "name": "pixfaro-docs-worker",
  "private": true,
  "scripts": {
    "gen": "node scripts/gen-content.mjs",
    "check": "npm run gen && git diff --exit-code -- src/content.generated.ts",
    "deploy": "npm run gen && npx wrangler deploy"
  },
  "devDependencies": {
    "marked": "^18.0.5"
  }
}
