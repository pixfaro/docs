/**
 * AUTO-GENERATED by scripts/gen-content.mjs — DO NOT EDIT.
 * Source of truth: api.md, mcp.md, llms.txt at the repo root.
 * Regenerate with `npm run gen` (runs automatically on deploy).
 */

/** Verbatim contents of /llms.txt. */
export const LLMS = "# Pixfaro\n> Every image model behind one API and one MCP: your agent asks for an image; Pixfaro holds the provider accounts, keys, retries and format differences, and bills one prepaid balance.\n## Docs\n- REST API: https://docs.pixfaro.com/api\n- MCP: https://docs.pixfaro.com/mcp\n## Endpoints\n- API base: https://api.pixfaro.com\n- MCP: https://mcp.pixfaro.com/mcp (streamable HTTP, OAuth 2.1 — for claude.ai / Claude Desktop connectors)\n- Local MCP (stdio): npx -y @pixfaro/mcp with env PIXFARO_KEY=pf_live_… (Claude Code, Cursor, Windsurf)\n## Auth\n- API keys (pf_live_…) come from the dashboard at https://pixfaro.com; Authorization: Bearer pf_live_…\n- Scopes: generate (default; images + balance) and full (adds billing, logos, brand-kit)\n- New accounts get $1 free credit; balance is prepaid via Stripe top-ups (no subscription)\n## Capabilities\n- POST /v1/images/generations {model, prompt, aspect_ratio?, resolution?, overlay?} -> {id: img_…, url, cost, balance_after} — sync, one image per request, hosted URL (never base64)\n- POST /v1/images/edits {model, image: img_…, instruction} -> same shape; omitted aspect_ratio keeps the source's shape; omitted resolution inherits (and bills at) the source's tier\n- GET /v1/models (public) -> [{id, best_for, p50_ms, price, prices per resolution tier, enabled}] — pick by best_for; enabled:false = coming soon\n- GET /v1/balance -> {balance} (USD decimal string)\n- Overlay branding: \"overlay\":\"default\" applies the account's saved brand kit (PUT /v1/brand-kit); or explicit {text|logo_id, position, opacity…}; logos via POST /v1/logos (transparent PNG ≤1MB)\n- Failed generations are never charged (502 body says \"charged\": false); 402 insufficient_balance includes balance, needed, topup_url\n- MCP tools: generate_image, edit_image, list_models, get_balance — replies carry the hosted URL + cost + remaining balance\n- CLI: npx pixfaro gen \"prompt\" -a 16:9 -o cover.png ; npx pixfaro models ; npx pixfaro balance ; npx pixfaro edit img_… \"instruction\"\n## Limits\n- 60 requests/min per key, 5 keys per account; 429 carries retry_after_s; prompts ≤4000 chars; money always USD decimal strings\n## Quickstart\n- MCP: claude mcp add pixfaro -e PIXFARO_KEY=pf_live_… -- npx -y @pixfaro/mcp ; then ask the agent for an image\n- Remote MCP: add https://mcp.pixfaro.com/mcp as a custom connector; OAuth signs you in\n- REST: get a key at https://pixfaro.com -> POST /v1/images/generations with Authorization: Bearer -> url in the response\n";

/** Rendered HTML body of the docs page (api + mcp sections). */
export const BODY = "<h2 id=\"api\">REST API <span class=\"chip model\">REST</span></h2>\n<p>Base URL <code>https://api.pixfaro.com</code>. JSON everywhere. Errors use one envelope:</p>\n<pre><code class=\"language-json\">{ &quot;error&quot;: { &quot;code&quot;: &quot;insufficient_balance&quot;, &quot;message&quot;: &quot;…&quot;, &quot;request_id&quot;: &quot;rq_a1c4f0&quot; } }\n</code></pre>\n<p>Every response carries an <code>x-request-id</code> header — include it when you contact\nsupport. Money is always a <strong>USD decimal string</strong> (<code>&quot;0.080&quot;</code>, never a float).\nTimes are ISO 8601 UTC.</p>\n<h3>Authentication</h3>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>Principal</th>\n<th>How</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>API key</td>\n<td><code>Authorization: Bearer pf_live_…</code></td>\n</tr>\n<tr>\n<td>MCP</td>\n<td>OAuth 2.1 at <code>https://mcp.pixfaro.com</code> (remote) or <code>PIXFARO_KEY</code> env (stdio)</td>\n</tr>\n</tbody></table></div>\n<p>Keys are created on the <a href=\"https://pixfaro.com\">dashboard</a>. Scopes: <code>generate</code>\n(default — image endpoints + balance) and <code>full</code> (adds billing, logos, and\nbrand-kit management).</p>\n<h3><span class=\"method\">POST</span> /v1/images/generations</h3>\n<p>Generate an image. Sync — the response is the finished image.</p>\n<pre><code class=\"language-json\">{\n  &quot;model&quot;: &quot;nano-banana-2&quot;,\n  &quot;prompt&quot;: &quot;a lighthouse at night, minimal flat style&quot;,\n  &quot;aspect_ratio&quot;: &quot;16:9&quot;,\n  &quot;resolution&quot;: &quot;1K&quot;,\n  &quot;overlay&quot;: &quot;default&quot;\n}\n</code></pre>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Required</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>model</code></td>\n<td>yes</td>\n<td>id from <code>GET /v1/models</code></td>\n</tr>\n<tr>\n<td><code>prompt</code></td>\n<td>yes</td>\n<td>1–4000 chars</td>\n</tr>\n<tr>\n<td><code>aspect_ratio</code></td>\n<td>no</td>\n<td><code>&quot;w:h&quot;</code>, e.g. <code>&quot;1:1&quot;</code> (default), <code>&quot;16:9&quot;</code>, <code>&quot;9:16&quot;</code></td>\n</tr>\n<tr>\n<td><code>resolution</code></td>\n<td>no</td>\n<td>per-model tiers from <code>GET /v1/models</code> <code>prices</code> (default <code>1K</code>)</td>\n</tr>\n<tr>\n<td><code>overlay</code></td>\n<td>no</td>\n<td>corner branding: <code>&quot;default&quot;</code> applies your saved brand kit, or an explicit object (below)</td>\n</tr>\n</tbody></table></div>\n<p><code>n</code> &gt; 1 and <code>response_format</code> other than <code>&quot;url&quot;</code> are not supported yet — one\nimage per request, delivered as a hosted URL (your agent&#39;s context never\ncarries base64).</p>\n<p><strong>200:</strong></p>\n<pre><code class=\"language-json\">{\n  &quot;id&quot;: &quot;img_8f2a…&quot;, &quot;url&quot;: &quot;https://api.pixfaro.com/i/…&quot;, &quot;model&quot;: &quot;nano-banana-2&quot;,\n  &quot;resolution&quot;: &quot;1K&quot;, &quot;latency_ms&quot;: 10400, &quot;cost&quot;: &quot;0.081&quot;, &quot;balance_after&quot;: &quot;12.32&quot;,\n  &quot;overlay_applied&quot;: false, &quot;request_id&quot;: &quot;rq_a1c4f0&quot;\n}\n</code></pre>\n<p><strong>Errors:</strong> <code>402 insufficient_balance</code> (body includes <code>balance</code>, <code>needed</code>,\n<code>topup_url</code>), <code>400 invalid_model</code> / <code>invalid_prompt</code> / <code>invalid_request</code>,\n<code>409 model_disabled</code>, <code>429 rate_limited</code> (+ <code>retry_after_s</code>),\n<code>502 provider_failed</code> — <strong>no charge</strong>, and the body says so explicitly:\n<code>&quot;charged&quot;: false</code>.</p>\n<h4>Overlay object</h4>\n<p>Brands a corner of the image with your handle or logo. Exactly one of <code>text</code>\n(≤ 64 chars) or <code>logo_id</code> (a <code>logo_…</code> from <code>POST /v1/logos</code>):</p>\n<pre><code class=\"language-json\">{ &quot;overlay&quot;: { &quot;text&quot;: &quot;@yourbrand&quot;, &quot;position&quot;: &quot;bottom-right&quot;, &quot;opacity&quot;: 0.9 } }\n</code></pre>\n<p>Optional: <code>position</code> (default <code>bottom-right</code>), <code>opacity</code> (0.2–1.0), <code>size</code>,\n<code>margin</code>, and for text <code>font</code> / <code>weight</code> / <code>color</code> (<code>&quot;auto&quot;</code> picks ink or paper\nper corner brightness). Save your defaults once via <code>PUT /v1/brand-kit</code>, then\n<code>&quot;overlay&quot;: &quot;default&quot;</code> everywhere. A bad overlay fails the request <strong>before</strong>\ngeneration — you are not charged.</p>\n<h3><span class=\"method\">POST</span> /v1/images/edits</h3>\n<p>Edit a previous generation with a natural-language instruction. Same response\nshape as generations.</p>\n<pre><code class=\"language-json\">{ &quot;model&quot;: &quot;nano-banana-2&quot;, &quot;image&quot;: &quot;img_8f2a…&quot;, &quot;instruction&quot;: &quot;make the sky darker&quot; }\n</code></pre>\n<ul>\n<li><code>image</code> takes an <code>img_…</code> id from a previous generation (URLs are not\naccepted).</li>\n<li><code>instruction</code>: 1–4000 chars — what to change; everything else stays put.</li>\n<li>Omitted <code>aspect_ratio</code> <strong>keeps the source image&#39;s shape</strong> (generations\ndefault to 1:1).</li>\n<li>Omitted <code>resolution</code> inherits — and bills at — the source image&#39;s tier.</li>\n</ul>\n<h3><span class=\"method\">GET</span> /v1/models</h3>\n<p>Public, no auth. Cached ~5 min.</p>\n<pre><code class=\"language-json\">[{ &quot;id&quot;: &quot;nano-banana-2&quot;, &quot;name&quot;: &quot;Nano Banana 2&quot;,\n   &quot;best_for&quot;: &quot;general-purpose images: blog art, social posts, mockups&quot;,\n   &quot;p50_ms&quot;: 10700, &quot;p95_ms&quot;: 13600, &quot;price&quot;: &quot;0.080&quot;,\n   &quot;prices&quot;: { &quot;1K&quot;: &quot;0.080&quot;, &quot;2K&quot;: &quot;0.121&quot;, &quot;4K&quot;: &quot;0.181&quot; },\n   &quot;mode&quot;: &quot;sync&quot;, &quot;enabled&quot;: true }]\n</code></pre>\n<p><code>price</code> is the 1K figure; <code>prices</code> maps every supported resolution tier to its\nretail price. <code>enabled: false</code> marks models that are coming soon (calling one\nreturns <code>409 model_disabled</code>).</p>\n<h3><span class=\"method\">GET</span> /v1/balance</h3>\n<pre><code class=\"language-json\">{ &quot;balance&quot;: &quot;12.32&quot; }\n</code></pre>\n<h3>Account endpoints (scope <code>full</code>)</h3>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>Endpoint</th>\n<th>What</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>POST /v1/topups</code></td>\n<td><code>{ &quot;amount_usd&quot;: 25 }</code> → <code>{ &quot;checkout_url&quot;: … }</code> (Stripe; integer 5–1000, default 10)</td>\n</tr>\n<tr>\n<td><code>GET/PATCH /v1/autoreload</code></td>\n<td>auto top-up: <code>{ enabled, threshold, amount, monthly_cap }</code> — explicit opt-in</td>\n</tr>\n<tr>\n<td><code>POST /v1/logos</code></td>\n<td>upload a transparent PNG (≤ 1 MB, ≤ 2048px/side, ≤ 10 live logos) — raw body or multipart</td>\n</tr>\n<tr>\n<td><code>GET /v1/logos</code> · <code>DELETE /v1/logos/:id</code></td>\n<td>list / remove logos</td>\n</tr>\n<tr>\n<td><code>GET/PUT/DELETE /v1/brand-kit</code></td>\n<td>saved overlay defaults used by <code>&quot;overlay&quot;: &quot;default&quot;</code></td>\n</tr>\n</tbody></table></div>\n<p>Top-ups, keys, and usage are also on the <a href=\"https://pixfaro.com/dashboard\">dashboard</a>.</p>\n<h3><span class=\"method\">POST</span> /v1/abuse-reports</h3>\n<p>Public, no auth — report a generated image that violates our\n<a href=\"https://pixfaro.com/acceptable-use\">acceptable use policy</a>:\n<code>{ &quot;image_url&quot;, &quot;reason&quot;, &quot;details&quot;?, &quot;reporter_email&quot;? }</code> — <code>image_url</code> must\nbe a Pixfaro-served image link (<code>…/i/…</code>).</p>\n<h3>Rate limits</h3>\n<p>Per key: 60 requests/min. Per account: 5 keys. <code>429</code> responses carry\n<code>retry_after_s</code>. Need more? Ask — limits are raised case-by-case.</p>\n<h3>Versioning</h3>\n<p>Path-versioned (<code>/v1/</code>). Additive changes (new optional fields) don&#39;t bump the\nversion; breaking changes go to <code>/v2/</code> with 6 months of <code>/v1/</code> support. The\n<code>x-pixfaro-deprecation</code> header announces sunsets ahead of time.</p>\n\n<h2 id=\"mcp\">MCP server <span class=\"chip model\">MCP</span></h2>\n<p>One MCP connection gives your agent every major image model. Two ways in:</p>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>Transport</th>\n<th>For</th>\n<th>How</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Remote</strong> (streamable HTTP + OAuth 2.1)</td>\n<td>claude.ai, Claude Desktop, any HTTP-capable client</td>\n<td>add <code>https://mcp.pixfaro.com/mcp</code> — the client runs the OAuth flow, you sign in with your Pixfaro account</td>\n</tr>\n<tr>\n<td><strong>Local</strong> (stdio, npm)</td>\n<td>Claude Code, Cursor, Windsurf, config-file setups</td>\n<td><code>npx -y @pixfaro/mcp</code> with <code>PIXFARO_KEY</code> in env</td>\n</tr>\n</tbody></table></div>\n<p>Source: <a href=\"https://github.com/pixfaro/mcp\">github.com/pixfaro/mcp</a> — the client is\nintentionally small enough to read before you hand it a key.</p>\n<h3>Setup</h3>\n<p><strong>Claude Code:</strong></p>\n<pre><code class=\"language-bash\">claude mcp add pixfaro -e PIXFARO_KEY=<span class=\"a\">pf_live_…</span> -- npx -y @pixfaro/mcp\n</code></pre>\n<p><strong>Cursor / Windsurf / Claude Desktop</strong> (config file):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;pixfaro&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@pixfaro/mcp&quot;],\n      &quot;env&quot;: { &quot;PIXFARO_KEY&quot;: &quot;<span class=\"a\">pf_live_…</span>&quot; }\n    }\n  }\n}\n</code></pre>\n<p><strong>claude.ai / Claude Desktop connector (remote, no key in a file):</strong> add custom\nconnector with URL <code>https://mcp.pixfaro.com/mcp</code>.</p>\n<h3>Tools</h3>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>generate_image</code></td>\n<td>prompt (+ optional <code>model</code>, <code>aspect_ratio</code>) → hosted image URL, with cost and remaining balance in the reply</td>\n</tr>\n<tr>\n<td><code>edit_image</code></td>\n<td>natural-language edit of a previous generation by its <code>img_…</code> id</td>\n</tr>\n<tr>\n<td><code>list_models</code></td>\n<td>live models with price, latency, and what each is best for</td>\n</tr>\n<tr>\n<td><code>get_balance</code></td>\n<td>current prepaid balance</td>\n</tr>\n</tbody></table></div>\n<p>Replies are agent-readable text carrying a hosted URL — never base64, so your\nagent&#39;s context stays small. When the balance runs low, replies append a\ntop-up warning.</p>\n<h3>Environment (stdio)</h3>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Meaning</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>PIXFARO_KEY</code></td>\n<td>API key (<code>pf_live_…</code>) from <a href=\"https://pixfaro.com\">pixfaro.com</a></td>\n</tr>\n<tr>\n<td><code>PIXFARO_API_URL</code></td>\n<td>endpoint override (staging / self-hosted)</td>\n</tr>\n</tbody></table></div>\n<h3>CLI</h3>\n<p>The same repo ships the <code>pixfaro</code> CLI for scripts, CI, and pipelines — the\nliteral one-command time-to-first-image:</p>\n<pre><code class=\"language-bash\">export PIXFARO_KEY=<span class=\"a\">pf_live_…</span>\nnpx pixfaro gen &quot;a lighthouse at night, minimal flat style&quot; -a 16:9 -o cover.png\nnpx pixfaro edit img_8f2a… &quot;make the sky darker&quot; -o v2.png\nnpx pixfaro models\nnpx pixfaro balance\n</code></pre>\n<p><code>-o</code> downloads the image; without it the CLI prints the hosted URL. A failed\ndownload never swallows the URL you already paid for.</p>";
