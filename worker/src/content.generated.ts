/**
 * AUTO-GENERATED by scripts/gen-content.mjs — DO NOT EDIT.
 * Source of truth: api.md, templates.md, mcp.md, llms.txt at the repo root.
 * Regenerate with `npm run gen` (runs automatically on deploy).
 */

/** Verbatim contents of /llms.txt. */
export const LLMS = "# Pixfaro\n> Every image model behind one API and one MCP: your agent asks for an image; Pixfaro holds the provider accounts, keys, retries and format differences, and bills one prepaid balance.\n## Docs\n- REST API: https://docs.pixfaro.com/api\n- Card templates: https://docs.pixfaro.com/templates\n- MCP: https://docs.pixfaro.com/mcp\n## Endpoints\n- API base: https://api.pixfaro.com\n- MCP: https://mcp.pixfaro.com/mcp (streamable HTTP, OAuth 2.1 — for claude.ai / Claude Desktop connectors)\n- Local MCP (stdio): npx -y @pixfaro/mcp with env PIXFARO_KEY=pf_live_… (Claude Code, Cursor, Windsurf)\n## Auth\n- API keys (pf_live_…) come from the dashboard at https://pixfaro.com; Authorization: Bearer pf_live_…\n- Scopes: generate (default; image endpoints only) and full (adds balance, billing, logos, brand-kit)\n- New accounts get $1 free credit on email verification (instant for Google/GitHub signups); balance is prepaid via Stripe top-ups (no subscription)\n- Generation requires a verified email (403 email_unverified before that)\n## Capabilities\n- POST /v1/images/generations {model, prompt, aspect_ratio?, resolution?, overlay?} -> {id: img_…, url, cost, balance_after} — sync, one image per request, hosted URL (never base64)\n- POST /v1/images/edits {model, image: img_…, instruction} -> same shape; omitted aspect_ratio keeps the source's shape; omitted resolution inherits (and bills at) the source's tier\n- GET /v1/models (public) -> [{id, best_for, p50_ms, price, prices per resolution tier, enabled}] — pick by best_for; enabled:false = coming soon\n- GET /v1/balance -> {balance} (USD decimal string; needs scope full)\n- Overlay branding: \"overlay\":\"default\" applies the account's saved brand kit (PUT /v1/brand-kit); or explicit {text|logo_id, position, opacity…}; logos via POST /v1/logos (transparent PNG ≤1MB)\n- Failed generations are never charged (502 body says \"charged\": false); 402 insufficient_balance includes balance, needed, topup_url\n- POST /v1/renders {template, slots, size?, style?, scale?, overlay?} -> {id: img_…, url, kind: \"render\", template, style, cost} — TYPESET cards (quote-card, post-card), flat $0.020, ~2s, sharp type; a render is a generation (same history, same refund rules)\n- GET /v1/templates (public) -> [{id, tier, price, sizes, slots:[{name,type,required,max}], styles:{palettes,fonts,layouts,shadow}}] — build a form from this\n- Card slots: text is rejected past its max (never truncated); image slots take ONLY ast_… ids, your own /i/ URLs, data URIs <=2MB, or \"default\" (brand identity) — arbitrary URLs are refused\n- style: omit for the default, \"auto\" (random, avoids your last combo on that template), \"brand\" (your saved palette), or {palette,font,layout,shadow}\n- POST /v1/assets {kind: avatar|logo|photo|image, data} -> {id: ast_…} (scope full; PNG/JPEG <=5MB); PUT /v1/brand-kit/identity {name,handle,avatar_asset_id,palette} powers \"default\" slots\n- MCP tools: generate_image, edit_image, render_template, upload_asset, list_models, list_templates, get_balance — replies carry the hosted URL + cost + remaining balance\n- CLI: npx pixfaro gen \"prompt\" -a 16:9 -o cover.png ; npx pixfaro models ; npx pixfaro balance ; npx pixfaro edit img_… \"instruction\"\n## Limits\n- Fair-use limits on auth/abuse endpoints (429 rate_limited); no fixed per-key cap; prompts ≤4000 chars; money always USD decimal strings\n## Quickstart\n- MCP: claude mcp add pixfaro -e PIXFARO_KEY=pf_live_… -- npx -y @pixfaro/mcp ; then ask the agent for an image\n- Remote MCP: add https://mcp.pixfaro.com/mcp as a custom connector; OAuth signs you in\n- REST: get a key at https://pixfaro.com -> POST /v1/images/generations with Authorization: Bearer -> url in the response\n";

/** Rendered HTML body of the docs page (api + templates + mcp sections). */
export const BODY = "<h2 id=\"api\">REST API <span class=\"chip model\">REST</span></h2>\n<p>Base URL <code>https://api.pixfaro.com</code>. JSON everywhere. Errors use one envelope:</p>\n<pre><code class=\"language-json\">{ &quot;error&quot;: { &quot;code&quot;: &quot;insufficient_balance&quot;, &quot;message&quot;: &quot;…&quot;, &quot;request_id&quot;: &quot;rq_a1c4f0&quot; } }\n</code></pre>\n<p>Every response carries an <code>x-request-id</code> header — include it when you contact\nsupport. Money is always a <strong>USD decimal string</strong> (<code>&quot;0.080&quot;</code>, never a float).\nTimes are ISO 8601 UTC.</p>\n<h3 id=\"authentication\">Authentication</h3>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>Principal</th>\n<th>How</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>API key</td>\n<td><code>Authorization: Bearer pf_live_…</code></td>\n</tr>\n<tr>\n<td>MCP</td>\n<td>OAuth 2.1 at <code>https://mcp.pixfaro.com</code> (remote) or <code>PIXFARO_KEY</code> env (stdio)</td>\n</tr>\n</tbody></table></div>\n<p>Keys are created on the <a href=\"https://api.pixfaro.com/dashboard\">dashboard</a>. Scopes:\n<code>generate</code> (default — image endpoints only) and <code>full</code> (adds balance, billing,\nlogos, and brand-kit management).</p>\n<p>New accounts must verify their email before generating — unverified calls\nreturn <code>403 email_unverified</code>. The $1 welcome credit lands on verification\n(instantly for Google/GitHub signups).</p>\n<h3 id=\"v1-images-generations\"><span class=\"method\">POST</span> /v1/images/generations</h3>\n<p>Generate an image. Sync — the response is the finished image.</p>\n<pre><code class=\"language-json\">{\n  &quot;model&quot;: &quot;nano-banana-2&quot;,\n  &quot;prompt&quot;: &quot;a lighthouse at night, minimal flat style&quot;,\n  &quot;aspect_ratio&quot;: &quot;16:9&quot;,\n  &quot;resolution&quot;: &quot;1K&quot;,\n  &quot;overlay&quot;: &quot;default&quot;\n}\n</code></pre>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Required</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>model</code></td>\n<td>yes</td>\n<td>id from <code>GET /v1/models</code></td>\n</tr>\n<tr>\n<td><code>prompt</code></td>\n<td>yes</td>\n<td>1–4000 chars</td>\n</tr>\n<tr>\n<td><code>aspect_ratio</code></td>\n<td>no</td>\n<td><code>&quot;w:h&quot;</code>, e.g. <code>&quot;1:1&quot;</code> (default), <code>&quot;16:9&quot;</code>, <code>&quot;9:16&quot;</code></td>\n</tr>\n<tr>\n<td><code>resolution</code></td>\n<td>no</td>\n<td>per-model tiers from <code>GET /v1/models</code> <code>prices</code> (default <code>1K</code>)</td>\n</tr>\n<tr>\n<td><code>overlay</code></td>\n<td>no</td>\n<td>corner branding: <code>&quot;default&quot;</code> applies your saved brand kit, or an explicit object (below)</td>\n</tr>\n</tbody></table></div>\n<p><code>n</code> &gt; 1 and <code>response_format</code> other than <code>&quot;url&quot;</code> are not supported yet — one\nimage per request, delivered as a hosted URL (your agent&#39;s context never\ncarries base64).</p>\n<p><strong>200:</strong></p>\n<pre><code class=\"language-json\">{\n  &quot;id&quot;: &quot;img_8f2a…&quot;, &quot;url&quot;: &quot;https://api.pixfaro.com/i/…&quot;, &quot;model&quot;: &quot;nano-banana-2&quot;,\n  &quot;resolution&quot;: &quot;1K&quot;, &quot;latency_ms&quot;: 10400, &quot;cost&quot;: &quot;0.080&quot;, &quot;balance_after&quot;: &quot;12.32&quot;,\n  &quot;overlay_applied&quot;: false, &quot;request_id&quot;: &quot;rq_a1c4f0&quot;\n}\n</code></pre>\n<p><strong>Errors:</strong> <code>402 insufficient_balance</code> (body includes <code>balance</code>, <code>needed</code>,\n<code>topup_url</code>), <code>400 invalid_model</code> / <code>invalid_prompt</code> / <code>invalid_request</code>,\n<code>403 email_unverified</code>, <code>409 model_disabled</code>, <code>429 rate_limited</code>,\n<code>502 provider_failed</code> — <strong>no charge</strong>, and the body says so explicitly:\n<code>&quot;charged&quot;: false</code>.</p>\n<h4 id=\"overlay-object\">Overlay object</h4>\n<p>Brands a corner of the image with your handle or logo. Exactly one of <code>text</code>\n(≤ 64 chars) or <code>logo_id</code> (a <code>logo_…</code> from <code>POST /v1/logos</code>):</p>\n<pre><code class=\"language-json\">{ &quot;overlay&quot;: { &quot;text&quot;: &quot;@yourbrand&quot;, &quot;position&quot;: &quot;bottom-right&quot;, &quot;opacity&quot;: 0.9 } }\n</code></pre>\n<p>Optional: <code>position</code> (default <code>bottom-right</code>), <code>opacity</code> (0.2–1.0; text\ndefaults to 0.9, logos to 1.0), <code>logo_style</code> (<code>sticker</code> — default — | <code>shadow</code>\n| <code>outline</code> | <code>none</code>), <code>size</code>,\n<code>margin</code>, and for text <code>font</code> / <code>weight</code> / <code>color</code> (<code>&quot;auto&quot;</code> picks ink or paper\nper corner brightness). Save your defaults once via <code>PUT /v1/brand-kit</code>, then\n<code>&quot;overlay&quot;: &quot;default&quot;</code> everywhere. A bad overlay fails the request <strong>before</strong>\ngeneration — you are not charged.</p>\n<h3 id=\"v1-images-edits\"><span class=\"method\">POST</span> /v1/images/edits</h3>\n<p>Edit a previous generation with a natural-language instruction. Same response\nshape as generations.</p>\n<pre><code class=\"language-json\">{ &quot;model&quot;: &quot;nano-banana-2&quot;, &quot;image&quot;: &quot;img_8f2a…&quot;, &quot;instruction&quot;: &quot;make the sky darker&quot; }\n</code></pre>\n<ul>\n<li><code>image</code> takes an <code>img_…</code> id from a previous generation (URLs are not\naccepted).</li>\n<li><code>instruction</code>: 1–4000 chars — what to change; everything else stays put.</li>\n<li>Omitted <code>aspect_ratio</code> <strong>keeps the source image&#39;s shape</strong> (generations\ndefault to 1:1).</li>\n<li>Omitted <code>resolution</code> inherits — and bills at — the source image&#39;s tier.</li>\n</ul>\n<h3 id=\"v1-models\"><span class=\"method\">GET</span> /v1/models</h3>\n<p>Public, no auth. Cached ~5 min.</p>\n<pre><code class=\"language-json\">[{ &quot;id&quot;: &quot;nano-banana-2&quot;, &quot;name&quot;: &quot;Nano Banana 2&quot;,\n   &quot;best_for&quot;: &quot;general-purpose images: blog art, social posts, mockups&quot;,\n   &quot;p50_ms&quot;: 10700, &quot;p95_ms&quot;: 13600, &quot;price&quot;: &quot;0.080&quot;,\n   &quot;prices&quot;: { &quot;1K&quot;: &quot;0.080&quot;, &quot;2K&quot;: &quot;0.121&quot;, &quot;4K&quot;: &quot;0.181&quot; },\n   &quot;mode&quot;: &quot;sync&quot;, &quot;enabled&quot;: true }]\n</code></pre>\n<p><code>price</code> is the 1K figure; <code>prices</code> maps every supported resolution tier to its\nretail price. <code>enabled: false</code> marks models that are coming soon (calling one\nreturns <code>409 model_disabled</code>).</p>\n<h3 id=\"v1-renders\"><span class=\"method\">POST</span> /v1/renders</h3>\n<p>Typeset a card from a template instead of generating one — flat $0.02, ~2s,\nsharp type. Full reference: <a href=\"#templates\">Card templates</a>.</p>\n<pre><code class=\"language-bash\">curl https://api.pixfaro.com/v1/renders -H &quot;Authorization: Bearer <span class=\"a\">pf_live_…</span>&quot; \\\n  -H &quot;Content-Type: application/json&quot; \\\n  -d &#39;{&quot;template&quot;:&quot;quote-card&quot;,&quot;slots&quot;:{&quot;quote&quot;:&quot;Ship it.&quot;,&quot;handle&quot;:&quot;@you&quot;},&quot;style&quot;:&quot;auto&quot;}&#39;\n</code></pre>\n<p>A render is a generation: it returns an <code>img_…</code>, lands in your history, and is\ndebited and refunded by the same rules.</p>\n<h3 id=\"v1-templates\"><span class=\"method\">GET</span> /v1/templates</h3>\n<p>Public, no auth — the card catalog with slots, style axes and price. See\n<a href=\"#templates\">Card templates</a>.</p>\n<h3 id=\"v1-balance\"><span class=\"method\">GET</span> /v1/balance</h3>\n<p>Requires scope <code>full</code> (a default <code>generate</code> key gets <code>403 insufficient_scope</code>).</p>\n<pre><code class=\"language-json\">{ &quot;balance&quot;: &quot;12.32&quot; }\n</code></pre>\n<h3 id=\"account-endpoints-scope-full\">Account endpoints (scope <code>full</code>)</h3>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>Endpoint</th>\n<th>What</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>POST /v1/topups</code></td>\n<td><code>{ &quot;amount_usd&quot;: 25 }</code> → <code>{ &quot;checkout_url&quot;: … }</code> (Stripe; integer 5–1000, default 10)</td>\n</tr>\n<tr>\n<td><code>GET/PATCH /v1/autoreload</code></td>\n<td>auto top-up: <code>{ enabled, threshold, amount, monthly_cap }</code> — explicit opt-in</td>\n</tr>\n<tr>\n<td><code>POST /v1/logos</code></td>\n<td>upload a transparent PNG (≤ 1 MB, ≤ 2048px/side, ≤ 10 live logos) — raw binary body, or JSON <code>{ &quot;image&quot;: &quot;&lt;base64 or data URI&gt;&quot;, &quot;name&quot;? }</code></td>\n</tr>\n<tr>\n<td><code>GET /v1/logos</code> · <code>DELETE /v1/logos/:id</code></td>\n<td>list / remove logos</td>\n</tr>\n<tr>\n<td><code>GET/PUT/DELETE /v1/brand-kit</code></td>\n<td>saved overlay defaults used by <code>&quot;overlay&quot;: &quot;default&quot;</code></td>\n</tr>\n<tr>\n<td><code>POST /v1/assets</code></td>\n<td>upload a PNG/JPEG (≤ 5 MB, ≤ 4096px/side, ≤ 100 live) for card slots — raw binary with <code>?kind=</code>, or JSON <code>{ &quot;kind&quot;, &quot;data&quot; }</code></td>\n</tr>\n<tr>\n<td><code>GET /v1/assets</code> · <code>DELETE /v1/assets/:id</code></td>\n<td>list / remove assets</td>\n</tr>\n<tr>\n<td><code>GET/PUT/DELETE /v1/brand-kit/identity</code></td>\n<td>name, handle, avatar and palette — what <code>&quot;default&quot;</code> resolves to in a card slot</td>\n</tr>\n</tbody></table></div>\n<p>Top-ups, keys, and usage are also on the <a href=\"https://api.pixfaro.com/dashboard\">dashboard</a>.</p>\n<h3 id=\"v1-abuse-reports\"><span class=\"method\">POST</span> /v1/abuse-reports</h3>\n<p>Public, no auth — report a generated image that violates our\n<a href=\"https://pixfaro.com/acceptable-use\">acceptable use policy</a>:\n<code>{ &quot;image_url&quot;, &quot;reason&quot;, &quot;details&quot;?, &quot;reporter_email&quot;? }</code> — <code>image_url</code> must\nbe a Pixfaro-served image link (<code>…/i/…</code>); <code>reason</code> is one of <code>csam</code>,\n<code>nonconsensual</code>, <code>violence_hate</code>, <code>infringement</code>, <code>other</code>.</p>\n<h3 id=\"rate-limits\">Rate limits</h3>\n<p>Fair-use limits apply on auth and abuse-prone endpoints (<code>429 rate_limited</code>).\nThere is no fixed per-key request cap today; sustained high volume is welcome —\ntalk to us if you&#39;re planning a big batch.</p>\n<h3 id=\"versioning\">Versioning</h3>\n<p>Path-versioned (<code>/v1/</code>). Additive changes (new optional fields) don&#39;t bump the\nversion; breaking changes go to <code>/v2/</code> with 6 months of <code>/v1/</code> support. The\n<code>x-pixfaro-deprecation</code> header announces sunsets ahead of time.</p>\n\n<h2 id=\"templates\">Card templates <span class=\"chip model\">CARDS</span></h2>\n<p>Not every image needs a model. A <strong>card</strong> is <em>typeset</em>, not generated: you send\ntext, we render it in a real browser and hand back a hosted PNG. Flat <strong>$0.02</strong>,\nabout two seconds, and the type comes out sharp — which is the one thing image\nmodels still get wrong.</p>\n<p>Same key, same balance, same MCP as everything else. A render returns an\n<code>img_…</code> at <code>/i/…</code>, shows up in your history, and can be downloaded, edited or\noverlaid like any other image.</p>\n<pre><code class=\"language-bash\">curl https://api.pixfaro.com/v1/renders \\\n  -H &quot;Authorization: Bearer <span class=\"a\">pf_live_…</span>&quot; -H &quot;Content-Type: application/json&quot; \\\n  -d &#39;{&quot;template&quot;:&quot;quote-card&quot;,\n       &quot;slots&quot;:{&quot;quote&quot;:&quot;Stop mistaking silence for nobody&#39;\\&#39;&#39;s home.&quot;,&quot;handle&quot;:&quot;@dashaworks&quot;},\n       &quot;style&quot;:&quot;auto&quot;}&#39;\n</code></pre>\n<pre><code class=\"language-json\">{ &quot;id&quot;: &quot;img_4e1807777fc895e5642a&quot;,\n  &quot;url&quot;: &quot;https://api.pixfaro.com/i/usr_…/img_….png&quot;,\n  &quot;kind&quot;: &quot;render&quot;, &quot;template&quot;: &quot;quote-card&quot;, &quot;size&quot;: &quot;16:9&quot;,\n  &quot;style&quot;: { &quot;palette&quot;: &quot;charcoal&quot;, &quot;font&quot;: &quot;schibsted&quot;, &quot;layout&quot;: &quot;bold-left&quot;, &quot;shadow&quot;: true },\n  &quot;cost&quot;: &quot;0.020&quot;, &quot;balance_after&quot;: &quot;12.30&quot;, &quot;latency_ms&quot;: 1412 }\n</code></pre>\n<h3 id=\"v1-templates-2\"><span class=\"method\">GET</span> /v1/templates</h3>\n<p>Public, no auth. Every template with its slots, style axes and exact price —\nenough to build a form from, which is what the dashboard does.</p>\n<pre><code class=\"language-json\">{ &quot;templates&quot;: [\n  { &quot;id&quot;: &quot;quote-card&quot;, &quot;name&quot;: &quot;Quote card&quot;, &quot;tier&quot;: &quot;simple&quot;, &quot;price&quot;: &quot;0.020&quot;,\n    &quot;best_for&quot;: &quot;one line worth quoting — hooks, testimonials, pull quotes&quot;,\n    &quot;sizes&quot;: [&quot;16:9&quot;, &quot;1:1&quot;, &quot;4:5&quot;, &quot;og&quot;],\n    &quot;slots&quot;: [ { &quot;name&quot;: &quot;quote&quot;, &quot;type&quot;: &quot;text&quot;, &quot;required&quot;: true, &quot;max&quot;: 280, &quot;hint&quot;: &quot;the line itself&quot; } ],\n    &quot;styles&quot;: { &quot;palettes&quot;: [&quot;paper&quot;, &quot;charcoal&quot;, …], &quot;fonts&quot;: [&quot;schibsted&quot;, …], &quot;layouts&quot;: [&quot;bold-left&quot;, …], &quot;shadow&quot;: true } } ] }\n</code></pre>\n<h3 id=\"v1-renders-2\"><span class=\"method\">POST</span> /v1/renders</h3>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>Field</th>\n<th>What</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>template</code></td>\n<td>id from <code>GET /v1/templates</code></td>\n</tr>\n<tr>\n<td><code>slots</code></td>\n<td>the template&#39;s slot values (below)</td>\n</tr>\n<tr>\n<td><code>size</code></td>\n<td><code>16:9</code> 1200×675 · <code>1:1</code> 1080×1080 · <code>4:5</code> 1080×1350 · <code>og</code> 1200×630 — default <code>16:9</code></td>\n</tr>\n<tr>\n<td><code>style</code></td>\n<td>omit for the template default, <code>&quot;auto&quot;</code>, <code>&quot;brand&quot;</code>, or an object of axes</td>\n</tr>\n<tr>\n<td><code>scale</code></td>\n<td>device pixel ratio: 1, 2 (default) or 3 (+$0.01)</td>\n</tr>\n<tr>\n<td><code>overlay</code></td>\n<td>optional corner branding, exactly as on a generation</td>\n</tr>\n</tbody></table></div>\n<p>Scope <code>generate</code> is enough — a render is a generation.</p>\n<h4 id=\"slots\">Slots</h4>\n<p>Text slots take a string and are rejected (never truncated) past their <code>max</code>,\nso you never pay for a card that quietly lost its last sentence. Image slots\ntake <strong>only</strong> Pixfaro-hosted references:</p>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>Value</th>\n<th>Meaning</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>&quot;ast_…&quot;</code> or <code>{ &quot;asset&quot;: &quot;ast_…&quot; }</code></td>\n<td>an asset you uploaded (<code>POST /v1/assets</code>)</td>\n</tr>\n<tr>\n<td><code>&quot;https://api.pixfaro.com/i/…&quot;</code></td>\n<td>one of your own generated images</td>\n</tr>\n<tr>\n<td><code>&quot;data:image/png;base64,…&quot;</code></td>\n<td>inline, ≤ 2 MB</td>\n</tr>\n<tr>\n<td><code>&quot;default&quot;</code></td>\n<td>from your brand identity (<code>PUT /v1/brand-kit/identity</code>)</td>\n</tr>\n</tbody></table></div>\n<p>Arbitrary URLs are refused: the renderer is a real browser, and a fetchable\nslot would make every card a probe into whatever that browser can reach.\n<code>&quot;default&quot;</code> also works on the <code>name</code> and <code>handle</code> text slots.</p>\n<h4 id=\"style\">Style</h4>\n<p><code>&quot;auto&quot;</code> picks a random combination and <strong>avoids the one you used last</strong> on this\ntemplate — two cards in a row that differ only in wording read as a bug, not as\na series. <code>&quot;brand&quot;</code> paints your saved colours (<code>identity.palette</code>). An object\npins whichever axes you name and defaults the rest:</p>\n<pre><code class=\"language-json\">{ &quot;style&quot;: { &quot;palette&quot;: &quot;charcoal&quot;, &quot;font&quot;: &quot;instrument&quot;, &quot;layout&quot;: &quot;block&quot;, &quot;shadow&quot;: true } }\n</code></pre>\n<p>The chosen combination comes back in the response, so a card you like can be\nre-rendered exactly.</p>\n<h4 id=\"errors\">Errors</h4>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>Code</th>\n<th>Meaning</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>invalid_template</code></td>\n<td>unknown id — see <code>GET /v1/templates</code></td>\n</tr>\n<tr>\n<td><code>invalid_slot</code></td>\n<td>with a <code>slot</code> field naming which one</td>\n</tr>\n<tr>\n<td><code>asset_not_found</code></td>\n<td>unknown, deleted, or not yours (404, never an existence oracle)</td>\n</tr>\n<tr>\n<td><code>render_busy</code></td>\n<td>429, renderer at capacity — retry; <strong>not charged</strong></td>\n</tr>\n<tr>\n<td><code>render_failed</code></td>\n<td>502; refunded, and <code>&quot;charged&quot;: false</code> says so</td>\n</tr>\n</tbody></table></div>\n<h3 id=\"v1-assets\"><span class=\"method\">POST</span> /v1/assets</h3>\n<p>Scope <code>full</code>. PNG or JPEG, ≤ 5 MB, ≤ 4096 px/side, 100 live per account. Raw\nbinary with <code>?kind=avatar</code>, or JSON:</p>\n<pre><code class=\"language-bash\">curl https://api.pixfaro.com/v1/assets \\\n  -H &quot;Authorization: Bearer <span class=\"a\">pf_live_…</span>&quot; -H &quot;Content-Type: application/json&quot; \\\n  -d &#39;{&quot;kind&quot;:&quot;avatar&quot;,&quot;data&quot;:&quot;data:image/png;base64,…&quot;}&#39;\n</code></pre>\n<p>→ <code>{ &quot;id&quot;: &quot;ast_…&quot;, &quot;kind&quot;: &quot;avatar&quot;, &quot;width&quot;: 512, &quot;height&quot;: 512 }</code></p>\n<p><code>GET /v1/assets?kind=avatar</code> lists yours; <code>DELETE /v1/assets/:id</code> removes one.\nAssets are private — they are never served at a public URL; the renderer reads\nthem server-side and inlines them into the card.</p>\n<p>Uploading a photo of a person means you have the right to use it. Personas are\nself-likeness only, and attributing words to someone who did not say them is\nimpersonation whatever the styling — see the\n<a href=\"https://pixfaro.com/acceptable-use\">acceptable use policy</a>.</p>\n<h3 id=\"brand-identity\">Brand identity</h3>\n<p><code>PUT /v1/brand-kit/identity</code> (scope <code>full</code>) is what <code>&quot;default&quot;</code> resolves to:</p>\n<pre><code class=\"language-json\">{ &quot;name&quot;: &quot;Dasha&quot;, &quot;handle&quot;: &quot;@dashaworks&quot;, &quot;avatar_asset_id&quot;: &quot;ast_…&quot;,\n  &quot;palette&quot;: { &quot;bg&quot;: &quot;#101014&quot;, &quot;ink&quot;: &quot;#fdfdfb&quot;, &quot;accent&quot;: &quot;#b4e33d&quot; } }\n</code></pre>\n<p>It sits beside the overlay brand kit (<code>/v1/brand-kit</code>) and is cleared\nseparately.</p>\n<h3 id=\"template-catalog\">Template catalog</h3>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>id</th>\n<th>Tier</th>\n<th>Slots</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>quote-card</code></td>\n<td>simple · $0.02</td>\n<td><code>quote</code>, <code>name?</code>, <code>handle?</code>, <code>avatar?</code></td>\n</tr>\n<tr>\n<td><code>post-card</code></td>\n<td>simple · $0.02</td>\n<td><code>text</code>, <code>name</code>, <code>handle?</code>, <code>platform?</code>, <code>date?</code>, <code>avatar?</code></td>\n</tr>\n</tbody></table></div>\n<p>More arrive with composition — an image slot you can fill with a generation in\nthe same request.</p>\n<h3 id=\"fonts\">Fonts</h3>\n<p>Cards are set in self-hosted <a href=\"https://openfontlicense.org/\">SIL OFL 1.1</a> faces —\nno font CDN in the render path, so a card is deterministic and its typography\ndoes not depend on a third party being up. The licence text ships beside every\nfamily we serve.</p>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>Face</th>\n<th>Class</th>\n<th>Cyrillic</th>\n<th>Licence</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Schibsted Grotesk</td>\n<td>sans</td>\n<td>—</td>\n<td>OFL-1.1</td>\n</tr>\n<tr>\n<td>Space Grotesk</td>\n<td>sans</td>\n<td>—</td>\n<td>OFL-1.1</td>\n</tr>\n<tr>\n<td>Manrope</td>\n<td>sans</td>\n<td>✓</td>\n<td>OFL-1.1</td>\n</tr>\n<tr>\n<td>Inter</td>\n<td>sans</td>\n<td>✓</td>\n<td>OFL-1.1</td>\n</tr>\n<tr>\n<td>Syne</td>\n<td>sans</td>\n<td>—</td>\n<td>OFL-1.1</td>\n</tr>\n<tr>\n<td>Bricolage Grotesque</td>\n<td>sans</td>\n<td>—</td>\n<td>OFL-1.1</td>\n</tr>\n<tr>\n<td>Fraunces</td>\n<td>serif</td>\n<td>—</td>\n<td>OFL-1.1</td>\n</tr>\n<tr>\n<td>Playfair Display</td>\n<td>serif</td>\n<td>✓</td>\n<td>OFL-1.1</td>\n</tr>\n<tr>\n<td>DM Serif Display</td>\n<td>serif</td>\n<td>—</td>\n<td>OFL-1.1</td>\n</tr>\n<tr>\n<td>Instrument Serif</td>\n<td>serif</td>\n<td>—</td>\n<td>OFL-1.1</td>\n</tr>\n<tr>\n<td>IBM Plex Mono</td>\n<td>mono</td>\n<td>✓</td>\n<td>OFL-1.1</td>\n</tr>\n</tbody></table></div>\n<p>Cyrillic text set in a Latin-only face is switched to the closest\nCyrillic-capable face of the same class (Manrope, Playfair Display or IBM Plex\nMono), rather than falling through to a system font mid-headline.</p>\n\n<h2 id=\"mcp\">MCP server <span class=\"chip model\">MCP</span></h2>\n<p>One MCP connection gives your agent every major image model. Two ways in:</p>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>Transport</th>\n<th>For</th>\n<th>How</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Remote</strong> (streamable HTTP + OAuth 2.1)</td>\n<td>claude.ai, Claude Desktop, any HTTP-capable client</td>\n<td>add <code>https://mcp.pixfaro.com/mcp</code> — the client runs the OAuth flow, you sign in with your Pixfaro account</td>\n</tr>\n<tr>\n<td><strong>Local</strong> (stdio, npm)</td>\n<td>Claude Code, Cursor, Windsurf, config-file setups</td>\n<td><code>npx -y @pixfaro/mcp</code> with <code>PIXFARO_KEY</code> in env</td>\n</tr>\n</tbody></table></div>\n<p>Source: <a href=\"https://github.com/pixfaro/mcp\">github.com/pixfaro/mcp</a> — the client is\nintentionally small enough to read before you hand it a key.</p>\n<h3 id=\"setup\">Setup</h3>\n<p><strong>Claude Code:</strong></p>\n<pre><code class=\"language-bash\">claude mcp add pixfaro -e PIXFARO_KEY=<span class=\"a\">pf_live_…</span> -- npx -y @pixfaro/mcp\n</code></pre>\n<p><strong>Cursor / Windsurf / Claude Desktop</strong> (config file):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;pixfaro&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@pixfaro/mcp&quot;],\n      &quot;env&quot;: { &quot;PIXFARO_KEY&quot;: &quot;<span class=\"a\">pf_live_…</span>&quot; }\n    }\n  }\n}\n</code></pre>\n<p><strong>claude.ai / Claude Desktop connector (remote, no key in a file):</strong> add custom\nconnector with URL <code>https://mcp.pixfaro.com/mcp</code>.</p>\n<h3 id=\"tools\">Tools</h3>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>generate_image</code></td>\n<td>prompt (+ optional <code>model</code>, <code>aspect_ratio</code>, <code>resolution</code>, <code>overlay</code> — incl. <code>&quot;default&quot;</code> for your saved brand kit) → hosted image URL, with cost and remaining balance in the reply</td>\n</tr>\n<tr>\n<td><code>edit_image</code></td>\n<td>natural-language edit of a previous generation by its <code>img_…</code> id (keeps the source&#39;s shape unless you pass <code>aspect_ratio</code>)</td>\n</tr>\n<tr>\n<td><code>render_template</code></td>\n<td>typeset card from a template — slots in, hosted PNG out (flat $0.02, ~2s); sharper type than any model, and cheaper</td>\n</tr>\n<tr>\n<td><code>upload_asset</code></td>\n<td>store a PNG/JPEG (≤ 5 MB) and get an <code>ast_…</code> id to pass into a card slot</td>\n</tr>\n<tr>\n<td><code>list_models</code></td>\n<td>live models with price, latency, and what each is best for</td>\n</tr>\n<tr>\n<td><code>list_templates</code></td>\n<td>card templates with their slots, style axes and exact price</td>\n</tr>\n<tr>\n<td><code>get_balance</code></td>\n<td>current prepaid balance — needs scope <code>full</code> (stdio: a full-scope key; remote OAuth grants <code>generate</code> by default)</td>\n</tr>\n</tbody></table></div>\n<p>Replies are agent-readable text carrying a hosted URL — never base64, so your\nagent&#39;s context stays small. When the balance runs low, replies append a\ntop-up warning.</p>\n<h3 id=\"environment-stdio\">Environment (stdio)</h3>\n<div class=\"tbl\"><table>\n<thead>\n<tr>\n<th>Variable</th>\n<th>Meaning</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>PIXFARO_KEY</code></td>\n<td>API key (<code>pf_live_…</code>) from <a href=\"https://pixfaro.com\">pixfaro.com</a></td>\n</tr>\n<tr>\n<td><code>PIXFARO_API_URL</code></td>\n<td>endpoint override (staging / self-hosted)</td>\n</tr>\n</tbody></table></div>\n<h3 id=\"cli\">CLI</h3>\n<p>The same repo ships the <code>pixfaro</code> CLI for scripts, CI, and pipelines — the\nliteral one-command time-to-first-image:</p>\n<pre><code class=\"language-bash\">export PIXFARO_KEY=<span class=\"a\">pf_live_…</span>\nnpx pixfaro gen &quot;a lighthouse at night, minimal flat style&quot; -a 16:9 -o cover.png\nnpx pixfaro edit img_8f2a… &quot;make the sky darker&quot; -o v2.png\nnpx pixfaro models\nnpx pixfaro balance\n</code></pre>\n<p><code>-o</code> downloads the image; without it the CLI prints the hosted URL. A failed\ndownload never swallows the URL you already paid for.</p>";
